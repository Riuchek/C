#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    /* Programa Calculador de custos
        Sabe-se que o custo de um carro novo ao consumidor é a soma do custo de fabrica com a porcentagem do distribuidor e dos impostos(aplicados ao custo de fabrica)
        Crie um programa que permia ao usuario informar o valor de custo de fabrica, o % de imposto e o % do distribuidor


        a saida devera mostrar 
        o valor total do carro
        o valor do distribuidor e a porcentagem informada
        o Valor dos impostos
    
     */
    return 0;
}
